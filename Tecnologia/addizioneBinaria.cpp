//
// Created by lucac on 11/7/2023.
//
#include <iostream>

using namespace std;

int main() {
    string bin1, bin2, presum, sum;
    fflush(stdin);
    getline(cin, bin1);
    fflush(stdin);
    getline(cin, bin2);
    for (int i = 0; i < 9; i++) {
        presum[i]=bin1[i]+bin2[i];
    }
    return 0;
}
